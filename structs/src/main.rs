fn main() {
    struct User {
        username: String,
        email: String,
        sign_in_account: u64,
        active: bool,
    };

    let user1 = User {
        email: String::from("example@xyz.com"),
    }
}
